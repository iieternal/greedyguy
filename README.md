# greedy-guy
A byte-prediction compression ecosystem. Includes a C-based compression engine, WASM builds for browser &amp; Node.js, and drop-in middleware for Express, Fastify, Electron, and Expo (React Native). Compress assets, cache responses and decompresses streams on the fly - everywhere Javascript runs.
